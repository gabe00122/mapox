[package]
name = "mapox-core"
description = "Multi-agent partially observable gridworld environments"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "mapox_core"

[dependencies]
fastnoise-lite.workspace = true
log.workspace = true
rand.workspace = true
ndarray.workspace = true
rayon.workspace = true
serde.workspace = true
serde_json.workspace = true

# eframe compiles for native (winit + wgpu) and wasm (web runner + wgpu)
# alike, so the demo app lives here once and each shell crate just runs it.
eframe.workspace = true
wgpu.workspace = true
egui.workspace = true
image.workspace = true

[dev-dependencies]
parking_lot.workspace = true
